# Pokemon Assessment

Monorepo scaffold for a Java 17 Spring Boot 3 backend and React TypeScript Vite frontend.

## Structure

- `backend`: Clean Architecture Spring Boot service.
- `frontend`: React + TypeScript + Vite client.
- `docker-compose.yml`: Local PostgreSQL service.
